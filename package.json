{
  "name": "azure-devops-git-clone",
  "displayName": "Azure DevOps Git Clone",
  "description": "Sign in to Azure DevOps and clone repositories with the native VS Code clone experience.",
  "version": "0.1.6",
  "publisher": "ashurtechnet",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/zacpr/azure-devops-git-clone"
  },
  "icon": "images/icon.png",
  "galleryBanner": {
    "color": "#0078D4",
    "theme": "dark"
  },
  "keywords": [
    "azure devops",
    "ado",
    "git",
    "clone",
    "repository"
  ],
  "qna": "https://github.com/zacpr/azure-devops-git-clone/issues",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "SCM Providers"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "adoGitClone.cloneRepo",
        "title": "Azure DevOps: Clone Git Repository",
        "icon": "$(cloud-download)"
      }
    ],
    "configuration": {
      "title": "Azure DevOps Git Clone",
      "properties": {
        "adoGitClone.defaultClonePath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Default parent folder for cloned repositories. Leave empty to default to the parent of the current workspace folder, or your home directory. Supports `~` for the home directory (e.g. `~/repos`)."
        }
      }
    },
    "menus": {
      "commandPalette": [
        {
          "command": "adoGitClone.cloneRepo"
        }
      ],
      "scm/title": [
        {
          "command": "adoGitClone.cloneRepo",
          "group": "navigation",
          "when": "config.git.enabled"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.2",
    "typescript": "^5.3.3"
  }
}
